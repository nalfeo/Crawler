# Icon Batch Generation Pipeline
#
# Generates and approves batched icon sheets (achievements + abilities). Icons
# are 128×128 pixel-art symbols on a transparent background — frames are
# composed separately as deterministic UI elements at render time.
#
# ─── Security posture (public repo!) ─────────────────────────────────────────
# Same model as asset-request.yml: issue-event runs require OWNER/MEMBER/
# COLLABORATOR author_association; workflow_dispatch is the maintainer escape
# hatch. Azure keys are wired only to the steps that need them.
# ─────────────────────────────────────────────────────────────────────────────
name: Icon Batch Pipeline

on:
  issues:
    types: [labeled, edited, reopened]
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        default: 'status'
        type: choice
        options:
          - generate-briefs
          - run
          - run-all
          - status
      batch_ids:
        description: 'Space-separated batch IDs to run (e.g. "achv-icons-batch-01 achv-icons-batch-02"). Only used for action=run.'
        required: false
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-worker
  cancel-in-progress: false
  queue: single

jobs:
  icon-batch:
    name: Icon batch — ${{ github.event_name == 'issues' && 'issue' || inputs.action }}
    runs-on: ubuntu-latest

    if: |
      github.event_name != 'issues' ||
      (
        github.event.action == 'labeled' &&
        github.event.label.name == 'icon-batch' &&
        (
          contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association) ||
          (github.event.issue.user.type == 'Bot' && contains(fromJSON('["Copilot","github-actions[bot]"]'), github.event.issue.user.login))
        )
      )

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./.github/actions/setup-node

      # ── Parse issue body ─────────────────────────────────────────────────────
      # GitHub issue forms render fields as "### Label\n\nvalue\n". We extract
      # action + batch_ids here so downstream steps never interpolate raw issue
      # body text into shell strings (eliminates the injection surface).
      - name: Parse issue body
        if: github.event_name == 'issues'
        id: parse_issue
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          ACTION=$(node -e "
            const body = process.env.ISSUE_BODY || '';
            const m = body.match(/###\s*Action\s*\n+([^\n#]+)/);
            process.stdout.write(m ? m[1].trim() : '');
          ")
          BATCH_IDS=$(node -e "
            const body = process.env.ISSUE_BODY || '';
            const m = body.match(/###\s*Batch IDs[^\n]*\n+([\s\S]*?)(?:\n###|$)/);
            const raw = m ? m[1].replace(/_No response_/g,'').trim() : '';
            process.stdout.write(raw);
          ")
          echo "action=$ACTION" >> "$GITHUB_OUTPUT"
          echo "batch_ids=$BATCH_IDS" >> "$GITHUB_OUTPUT"
          echo "Parsed from issue body — action=$ACTION batch_ids=$BATCH_IDS"

      # ── Generate briefs ──────────────────────────────────────────────────────
      - name: Generate icon briefs
        if: |
          (github.event_name == 'workflow_dispatch' && inputs.action == 'generate-briefs') ||
          (github.event_name == 'issues' && steps.parse_issue.outputs.action == 'generate-briefs')
        env:
          GH_TOKEN: ${{ secrets.CRAWLER_CI_PAT }}
        run: |
          npm run sprites:gen-achievement-icon-briefs
          npm run sprites:gen-ability-icon-briefs
          # Persist the generated briefs by committing and pushing to the branch.
          # Without this, later run/run-all dispatches start from a fresh checkout
          # with no briefs and fail. The push uses CRAWLER_CI_PAT so GitHub Actions
          # schedules subsequent CI runs correctly.
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add briefs/icons/
          if git diff --cached --quiet; then
            echo "generate-briefs: no new briefs generated (all already present)" >> "$GITHUB_STEP_SUMMARY"
          else
            BRIEF_COUNT=$(git diff --cached --name-only | grep '\.yaml$' | wc -l | tr -d ' ')
            git commit -m "chore(icons): generate icon batch briefs (${BRIEF_COUNT} files)"
            git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git"
            git push origin HEAD:"${{ github.ref_name }}"
            echo "generate-briefs: committed and pushed ${BRIEF_COUNT} brief file(s)" >> "$GITHUB_STEP_SUMMARY"
          fi

      # ── Status ───────────────────────────────────────────────────────────────
      - name: Status
        if: |
          (github.event_name == 'workflow_dispatch' && inputs.action == 'status') ||
          (github.event_name == 'issues' && steps.parse_issue.outputs.action == 'status')
        run: npm run sprites:icon-batch -- status | tee -a "$GITHUB_STEP_SUMMARY"

      # ── Run specific batches ─────────────────────────────────────────────────
      - name: Run specified batches
        if: |
          (github.event_name == 'workflow_dispatch' && inputs.action == 'run') ||
          (github.event_name == 'issues' && steps.parse_issue.outputs.action == 'run')
        env:
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
          AZURE_OPENAI_IMAGE_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_IMAGE_DEPLOYMENT }}
          AZURE_OPENAI_CHAT_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_CHAT_DEPLOYMENT }}
          AZURE_OPENAI_VISION_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_VISION_DEPLOYMENT }}
          SPRITES_PROVIDER: azure-openai
          SPRITES_TEXT_PROVIDER: azure-openai
          SPRITES_VISION_PROVIDER: azure-openai
          SPRITES_ALLOW_CI_PIPELINE: 'true'
          SPRITES_ALLOW_CI_ICON_BATCH_PUBLISH: 'true'
          GH_TOKEN: ${{ secrets.CRAWLER_CI_PAT }}
          AZURE_STORAGE_ACCOUNT: ${{ secrets.AZURE_STORAGE_ACCOUNT }}
          AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          AZURE_STORAGE_RUNS_CONTAINER: ${{ secrets.AZURE_STORAGE_RUNS_CONTAINER }}
          SPRITES_RUN_STORE: ${{ secrets.SPRITES_RUN_STORE || 'azure-blob' }}
          # All user-controlled values are passed through env (never interpolated
          # into the shell script) to eliminate shell-injection risk.
          WF_BATCH_IDS: ${{ inputs.batch_ids }}
          ISSUE_BATCH_IDS: ${{ steps.parse_issue.outputs.batch_ids }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # batch_ids: workflow_dispatch input takes precedence; fall back to
          # parsed issue output. Both arrive as env vars — never interpolated
          # into the script body — so they cannot break out of this string context.
          BATCH_IDS="$WF_BATCH_IDS"
          if [ -z "$BATCH_IDS" ] && [ "$EVENT_NAME" = "issues" ]; then
            BATCH_IDS="$ISSUE_BATCH_IDS"
          fi
          if [ -z "$BATCH_IDS" ]; then
            echo "ERROR: action=run requires batch_ids (workflow_dispatch input or Batch IDs field in issue body)" >&2
            exit 1
          fi

          # Validate each batch ID: must be a kebab-case slug (letters, digits,
          # hyphens only). Rejects any value that could escape the find pattern.
          BRIEF_ARGS=""
          for BATCH_ID in $BATCH_IDS; do
            if ! echo "$BATCH_ID" | grep -qE '^[a-z0-9][a-z0-9-]*[a-z0-9]$'; then
              echo "ERROR: invalid batch_id '${BATCH_ID}' — must be kebab-case (letters, digits, hyphens)" >&2
              exit 1
            fi
            BRIEF=$(find briefs/icons -name "${BATCH_ID}.yaml" 2>/dev/null | head -1)
            if [ -z "$BRIEF" ]; then
              echo "WARNING: brief not found for batch_id=${BATCH_ID} — skipping" >&2
              continue
            fi
            BRIEF_ARGS="${BRIEF_ARGS} --brief ${BRIEF}"
          done
          if [ -z "$BRIEF_ARGS" ]; then
            echo "ERROR: no valid briefs found for the specified batch_ids" >&2
            exit 1
          fi

          npm run sprites:icon-batch -- run $BRIEF_ARGS
          echo "run: completed for batch_ids: $BATCH_IDS" >> "$GITHUB_STEP_SUMMARY"

      # ── Run all pending batches ──────────────────────────────────────────────
      - name: Run all batches
        if: |
          (github.event_name == 'workflow_dispatch' && inputs.action == 'run-all') ||
          (github.event_name == 'issues' && steps.parse_issue.outputs.action == 'run-all')
        env:
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
          AZURE_OPENAI_IMAGE_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_IMAGE_DEPLOYMENT }}
          AZURE_OPENAI_CHAT_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_CHAT_DEPLOYMENT }}
          AZURE_OPENAI_VISION_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_VISION_DEPLOYMENT }}
          SPRITES_PROVIDER: azure-openai
          SPRITES_TEXT_PROVIDER: azure-openai
          SPRITES_VISION_PROVIDER: azure-openai
          SPRITES_ALLOW_CI_PIPELINE: 'true'
          SPRITES_ALLOW_CI_ICON_BATCH_PUBLISH: 'true'
          GH_TOKEN: ${{ secrets.CRAWLER_CI_PAT }}
          AZURE_STORAGE_ACCOUNT: ${{ secrets.AZURE_STORAGE_ACCOUNT }}
          AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          AZURE_STORAGE_RUNS_CONTAINER: ${{ secrets.AZURE_STORAGE_RUNS_CONTAINER }}
          SPRITES_RUN_STORE: ${{ secrets.SPRITES_RUN_STORE || 'azure-blob' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          npm run sprites:icon-batch -- run-all
          echo "run-all: completed" >> "$GITHUB_STEP_SUMMARY"
