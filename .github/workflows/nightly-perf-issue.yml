name: Nightly Perf Issue

on:
  schedule:
    - cron: '35 8 * * *'
  workflow_dispatch:

concurrency:
  group: nightly-perf-issue-filer
  cancel-in-progress: false

permissions:
  contents: read
  issues: write

jobs:
  file-issue:
    name: File nightly perf issue
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout trusted filer
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false

      - name: File nightly perf issue
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CRAWLER_CI_PAT: ${{ secrets.CRAWLER_CI_PAT }}
          LIFECYCLE_MUTATION_OWNER: ${{ vars.LIFECYCLE_MUTATION_OWNER }}
        run: node .github/scripts/nightly-perf-issue/run.mjs
