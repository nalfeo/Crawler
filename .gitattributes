# Shell scripts must always check out with LF endings.
# Without this, Windows users with core.autocrlf=true get CRLF and
# scripts like scripts/agent/lab-gate-check.sh fail in bash with
# "invalid option name" on `set -euo pipefail`.
*.sh text eol=lf
