{
  "date": "2026-07-02",
  "slug": "combat-perf-melee-broadphase",
  "task": "Convert meleeSwingSystem hit-detection from a full-[Health,Position] scan to a spatial-hash queryRadius broad-phase (identical-by-construction), reusing the collisionSystem grid. This is the melee increment (3 apples) of the combined combat-perf PR #678; the beam half was ultimately folded into the SAME PR via Path A (see 2026-07-02-combat-perf-beam-broadphase.json) — it was NOT shipped as a separate fast-follow.",
  "estimated_apples": 3,
  "actual_apples": 3,
  "verdict": "exact",
  "notes": "Deterministic src/core hot-path optimization with a hard identical-by-construction requirement (applyDamage draws world.rng per qualifying hit, so hit ORDER is determinism-observable). Design: superset grid.queryRadius broad-phase + byte-identical legacy narrow-phase + once-per-frame canonical rank map preserving bitecs dense-array iteration order, plus an executable full-scan fallback for sprite-less-Health worlds. Estimate held exactly: plan review (gpt-5.4) resolved 9 concerns, the melee half was scoped and reviewed as a standalone 3-apple increment, and code review (2 rounds, claude-sonnet-4.6) found no substantive defects. The one late addition — a meleeBroadPhase guard seam on SimulationOptions + a full-pipeline headless differential test — was a review-driven future-proofing item (rule #10) that stayed within the 3-apple envelope. Two permanent determinism regression tests (unit lockstep + full-pipeline). Bench: grid 2.7x-7.95x faster at Floor-2 spread scale, near-parity in the pathological dense-cluster worst case. Engagement budget (ADR 0024) validated at scale, no gameplay change. RECONCILIATION: an intermediate plan (Path B) would have shipped beam as a separate fast-follow, but the human ultimately chose Path A — folding the beam conversion + knockback coupling into the SAME combined PR #678 under the full 4-apple harness. Nothing was deferred; see the sibling record 2026-07-02-combat-perf-beam-broadphase.json and the review ledger for the combined 4-apple scope."
}
