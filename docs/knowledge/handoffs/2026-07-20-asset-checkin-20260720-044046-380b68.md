# Handoff: asset-checkin consolidation (void-rapier queue wave)

## Date

2026-07-20

## Persona

Producer

## Systems touched

sprite-pipeline

## Apples

1🍎 estimated, 1🍎 actual (art-surface-only consolidation)

## What was done

- Consolidated the currently open `asset-checkin` queue wave (12 issues) into this branch by applying the approved generated art surface from each check-in branch:
  - #1710, #1712, #1714, #1716, #1718, #1720, #1722, #1723, #1724, #1727, #1729, #1731
  - Source mapping:
    - #1710 → `generated/iron-cleaver-v1-var-0.png`
    - #1712 → `generated/iron-cleaver-v1-var-5.png`
    - #1714 → `generated/iron-cleaver-v1-var-10.png`
    - #1716 → `generated/dueling-saber-v1-var-3.png`
    - #1718 → `generated/void-rapier-v1-var-1.png`
    - #1720 → `generated/chain-hauberk-v3-var-1.png`
    - #1722 → `generated/chain-hauberk-v3-var-3.png`
    - #1723 → `generated/chain-hauberk-v3-var-7.png`
    - #1724 → `generated/chain-hauberk-v3-var-11.png`
    - #1727 → `generated/cactusfolk-boss-v1-var-3.png`
    - #1729 → `generated/cactusfolk-boss-v1-var-2.png`
    - #1731 → `generated/cactusfolk-boss-v1-var-0.png`
  - Added 12 PNGs under `public/assets/generated/**`
  - Merged `public/assets/generated/manifest.json` with deterministic union semantics
  - Merged `src/shared/data/sprite-catalog.json` with deterministic union semantics
- Specifically includes issue #1718 asset `generated/void-rapier-v1-var-1.png` and 11 additional queued approved assets.

## Validation

- `bash scripts/agent/preflight.sh`
- `npm run verify:fast` (before changes) ✅
- `npm run verify:fast` (after changes) ✅
- Deterministic spot-check script verified each expected asset has:
  - PNG present
  - matching `manifest.entries` record
  - matching `sprite-catalog` entry
- `npm run verify:pr-prereqs` (after handoff creation) ✅

## Blockers / notes

- Direct `gh` writes are blocked in this environment (`HTTP 403: Blocked by DNS monitoring proxy`), so:
  - Could not post the requested pre-code plan comment directly on issue #1718 from this runner.
  - Could not run the full `asset-pr` GH workflow path (issue listing/PR creation/auto-merge arming) from CLI.
- `npm run sprites:asset-pr` also hard-refuses when `CI` is set, so this session used the documented playbook-style manual fallback for the art-surface consolidation itself.

## Next steps

1. From an environment with GitHub write access, post the pre-code plan on #1718 for audit completeness.
2. Open/refresh the consolidated art-only PR (if not already opened by automation) and ensure description includes:
   - high-level summary
   - source check-in issue list with `Closes #...`
3. Arm auto-merge with `gh pr merge <n> --auto --squash`.
4. After merge: run `npm run sprites:generate-wiring -- --since main` and handle any follow-up non-art wiring PR separately.
