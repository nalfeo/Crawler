# Handoff: echo-bell Floor 2 weapon sprite — wiring and brief

**Date:** 2026-07-18
**Session slug:** echo-bell-sprite-wiring
**Closes:** Prepares #1360 (art generation queued; real art PR follows)
**Aggregate tracking:** #1303
**Apples:** 2🍎 estimated → 2🍎 actual
**PR:** (open)

## Systems touched

sprites, items

## What was done

Added the `echo-bell` weapon to the item catalog, generated a procedural placeholder
sprite, and authored the brief so the asset-request pipeline can generate real art.

### Changes

| File                                                                                  | Change                                                                              |
| ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `src/shared/items.ts`                                                                 | Added `echo-bell` (Echo Bell, Rare weapon) to `ITEM_CATALOG`                        |
| `tests/unit/items.test.ts`                                                            | Updated snapshot counts (catalog: 126→127, Weapons: 23→24)                          |
| `briefs/weapons/echo-bell.yaml`                                                       | New brief: vertical beam weapon, bell-on-handle silhouette, Floor 2 intensity       |
| `plans/item-icons/weapons.art.yaml`                                                   | Added echo-bell art-plan entry (placeholderInUse: true)                             |
| `public/assets/generated/echo-bell-placeholder.png`                                   | 16×16 procedural placeholder PNG                                                    |
| `public/assets/generated/manifest.json`                                               | Placeholder manifest entry (briefId: echo-bell, sourceRun: placeholder)             |
| `public/assets/generated/*-placeholder.png` (×11)                                     | Other missing placeholders generated by sprites:gen-placeholders (pre-existing gap) |
| `docs/knowledge/review-ledgers/2026-07-18-echo-bell-sprite-wiring.review-ledger.json` | 2🍎 ledger                                                                          |

### Wiring contract

- **Item resolution:** `resolveItemSprite('echo-bell')` auto-matches `briefId: 'echo-bell'` in
  the manifest — no explicit code wiring required (ADR 0051 / manifest-only path).
- **Stable ID:** `weapon.echo-bell`
- **Runtime key:** `equipment/weapon/echo-bell`
- **Production wave:** `floor2-equipment-weapon-beam`

### Art generation (not done in this session)

Azure OpenAI credentials are not available in the CI/cloud-agent environment.
The real sprite will be generated via the `asset-request` GitHub Actions workflow
(re-triggered by this PR's reference to #1360). After the workflow completes and
a maintainer approves a variant, the real art lands via a separate art-only PR
that closes #1360 and retires the placeholder.

## Verification

- `verify:fast` → ✅ all 1260 tests passed
- `lab-gate-check.sh` → ✅ passed
- `sprites:placeholder-audit` → echo-bell correctly appears as "placeholder-only"
- `sprites:gen-placeholders` (dry-run) → echo-bell entry skipped (already exists)
- `npm run review:ledger -- validate` → ✅ valid 2🍎 ledger

## Non-goals (deferred)

- Real generated PNG art (requires Azure credentials; queued via asset-request workflow)
- Equipment stat/balance tuning for Floor 2 (separate gameplay PR)
- Wiring echo-bell to a Floor 2 loot table (separate game-design PR)

## Recommended next steps

1. Merge this PR to land the placeholder + wiring infrastructure.
2. After the `asset-request` workflow (#1360) generates variants, approve a winner
   via `npm run sprites:approve` and check in via `npm run sprites:checkin` on a
   local dev box with Azure credentials.
3. Run `npm run sprites:asset-pr` to batch the checked-in art into an art-only PR
   that closes #1360.
