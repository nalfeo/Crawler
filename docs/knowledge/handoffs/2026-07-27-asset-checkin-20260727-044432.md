# Handoff: Asset check-in 20260727-044432 (57 approved assets)

**Date:** 2026-07-27  
**Session:** asset-pr consolidation for issue #2091  
**Apple estimate:** 🍎 1 (art/tooling-only, ceremony capped at 3🍎)

## Systems touched

sprites, welcome-room

## What was done

Consolidated open asset-checkin issue #2091 (57 approved sprites) into a single
batch PR using the `asset-pr` skill.

**Key facts:**
- 56 of the 57 assets were already present in `main`'s manifest from prior
  check-in merges; only `welcome-room-stanchion-pair-var-4` is net-new in this
  batch branch.
- The `sprites:asset-pr` script created branch `assets/batch-20260727-044648`
  and opened PR #2093.
- nalfeo pushed an additional commit to the batch branch adding the brief
  (`briefs/props/welcome-room-stanchion-pair.yaml`) and locked palette
  (`data/palettes/welcome-room-stanchion.json`), which causes the full CI suite
  to run (rather than the art-only fast lane).
- CI on PR #2093 was mostly green at session end (Unit, Lightweight, Integration,
  E2E Visual — Asset Smoke, E2E Visual — Devtools, Security all passed;
  Headless Floor 1 Gate, E2E Visual — Game/UI, Advisory coverage still running).
- The merge train is enabled (`MERGE_TRAIN_ENABLED=true`); it will promote
  PR #2093 when all admission checks pass. Do NOT arm `--auto --squash` —
  `reconcile-lib.mjs` blocks and disables any armed auto-merge to avoid
  out-of-order promotion.

## Post-merge wiring needed

### Equipment items — no wiring needed

All 18 equipment items in this check-in resolve automatically via
`itemId === briefId` (manifest lookup):

blood-lance, brass-knuckles, crystal-cannon, duelist-gloves, flame-tongs,
frost-crook, hunting-bola, plague-censer, quartermaster-cap, quarterstaff,
ritual-dagger, rune-axe, sawblade-launcher, shock-baton, tinker-grips,
velvet-coat, war-pick, warding-bell.

### Welcome-room props — wiring PR needed

These sprites are in the manifest but have **no consumer** in any set-piece
definition or runtime code:

| Sprite | Status |
|--------|--------|
| `welcome-room-stanchion-pair-var-4` | New in this PR; not in any set-piece |
| `welcome-room-floor-seam` (3 variants) | Already in main; no consumer |
| `welcome-room-floor-stain` (3 variants) | Already in main; no consumer |

**Action required after PR #2093 merges:**

1. Run `npm run sprites:generate-wiring -- --since main` to confirm the
   wiring opportunities (requires node_modules).
2. Add props to `src/shared/data/set-pieces.json` (welcome-room set piece) for
   the stanchion-pair, floor-seam, and floor-stain. Use `source: 'catalog'`
   with `spriteId: '<sprite-name>'` (e.g. `welcome-room-stanchion-pair-var-4`).
3. Open a separate non-art PR with the set-piece changes for full CI gates.

The welcome-room set piece currently references `welcome-room-velvet-rope-var-2`
as a catalog sprite — use the same pattern for the new props.

## PRs

- **PR #2093** — batch art PR (57 assets): `assets/batch-20260727-044648` →
  `main`. CI running; merge train will promote. Closes #2091.
- **PR #2092** — this session's tracking PR (no code changes).

## Files changed in PR #2093

- `public/assets/generated/welcome-room-stanchion-pair-var-4.png` (added)
- `public/assets/generated/manifest.json` (updated — 1 new entry)
- `src/shared/data/sprite-catalog.json` (updated — 1 new catalog entry)
- `briefs/props/welcome-room-stanchion-pair.yaml` (added by nalfeo)
- `data/palettes/welcome-room-stanchion.json` (added by nalfeo)
