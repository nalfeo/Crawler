# 2026-07-31 — PR #2414 CI recovery

## Systems touched

ci, sprites

## Summary

- Recovered dormant PR #2414 by syncing its branch with latest `main` and removing unintended drift so the PR no longer carries unrelated file changes.
- Verified the checked-in asset wiring path: shard key `tile-door-open-side-v1-var-0` maps to `assetPath: generated/tile-door-open-side-v1-var-0.png`, and engine boot preloads generated entries via `fetchGeneratedSpriteRegistry` + `preloadGeneratedSprites`.
- Confirmed the PNG exists on disk and is non-placeholder (`83x128`, non-empty IDAT payload).
- Replied in the summon thread with the required `✅ Addressed in <sha>` marker.

## Files touched

- `docs/knowledge/handoffs/2026-07-31-pr2414-ci-recovery.md`

## Verification run

- `npm run verify:fast` ✅
- `npm run verify:pr-prereqs` ✅ (after adding this handoff)
- Branch workflow inspection via GitHub Actions MCP (`list_workflow_runs` + `get_job_logs`) — no failed jobs found.

## Unresolved issues

- Could not arm auto-merge from this environment because `gh` is not authenticated against a GitHub host for this checkout (`gh pr merge --auto --squash` exits with host/auth error).

## Recommended next steps

- Arm auto-merge for PR #2414 from an authenticated environment, or close the PR if the zero-file diff remains intentional.
