# Handoff: PR #1231 CI recovery

## Date

2026-07-22

## Persona

DevOps Engineer

## Systems touched

ci-policy, sprite-workflow, mapgen

## Apples

Estimated 2🍎, actual 2🍎.

## What changed

- Investigated the failing PR run `29913411577` with the GitHub Actions MCP logs and traced the red `ci` / `Merge gate` jobs to the underlying `Lightweight Checks` failure.
- Restored 25 unrelated formatting-drift files back to their exact `origin/main` content so `format:check` no longer sees branch-local style churn.
- Removed two dead exported surfaces that Knip flagged but which were only used inside their own files: `computeCodeProvenance(...)` in the sprite sidecar service manager and `FLOOR2_SETTLEMENT_NPC_SPACING_TILES` in `floor2Settlement.ts`.
- Deleted the unreferenced `src/shared/data/floor2-equipment-art.ts` file that `lint:dead-code` reported as an unused file.
- Added this session's required 2🍎 tier-only review ledger.

## Observe before done

- Before: GitHub Actions run `29913411577` failed `Lightweight Checks`, with `format:check` reporting 26 files and `lint:dead-code` reporting one unused file plus two unused exports; the wrapper `ci` and `Merge gate` jobs failed only because they consume that result.
- After: the branch no longer carries the formatting drift that produced those 26 `prettier --check` failures, and the three dead-code findings are removed from the source tree.
- Real artifact: GitHub Actions job logs for `Lightweight Checks` (`88901614580`), `Merge gate` (`88903810403`), and `ci` (`88903827952`).

## Verification

- `github actions list_workflow_runs` for branch `nalfeo-fix-bat-ranged-dodging`
- `github get_job_logs` for jobs `88901614580`, `88903810403`, and `88903827952`
- `git diff refs/remotes/origin/main -- <restored files>` (confirmed the 25 formatting-fix files now match `origin/main`)
- `rg "floor2-equipment-art|computeCodeProvenance|FLOOR2_SETTLEMENT_NPC_SPACING_TILES"` (confirmed no remaining external references to the deleted file or de-exported symbols)

## Notes

- Local `npm ci`, `npm run format:check`, and `npm run lint:dead-code` could not be re-run in this sandbox because the package feed host `ms-feed-12.pkgs.visualstudio.com` was unreachable (`getaddrinfo ENOTFOUND`). CI is expected to provide the first full post-fix execution once this repair is pushed.
