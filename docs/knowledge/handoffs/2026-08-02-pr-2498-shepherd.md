# Handoff: PR #2498 Shepherd — 2026-08-02

## Summary

Shepherded PR #2498 (`assets/batch-20260731-211311`) — "feat(sprites): add 26 approved assets (66 check-ins)" — to a clean repair commit ready for merge.

## What was done

### Root causes diagnosed

CI run 30768351907 showed 4 distinct job failures all rooted in 2 underlying issues:

**Issue 1: 4 stale `contentHash` values in welcome-room prop shard JSONs**
- The batch merge of 65 orphaned checkin branches updated the PNG files for 4 welcome-room props but retained stale hashes in their shard JSONs
- Affected: `welcome-room-bookcase-var-0.json`, `welcome-room-desk-var-0.json`, `welcome-room-shop-table-var-0.json`, `welcome-room-velvet-rope-var-2.json`
- Failed: `Lightweight Checks` (check:asset-integrity), `Unit Tests` (asset-integrity.test.ts), `Advisory coverage`

**Issue 2: `widthFt: 6.49` for shop-table vs drawn 5.93ft**
- `welcome-room-shop-table-var-0.json` has invalid opaque bounds (`y:8 + height:82 = 90 > canvasHeight:85`)
- `resolveOpaqueFit` falls back to full-canvas scaling: `scale = heightFt/canvasHeight = 3.5/85`
- Drawn width = `3.5 * 144/85 = 5.93ft`, but both `welcome-room` and `welcome-room-v2` declared `6.49ft`
- Failed: `Unit Tests` (set-piece-declared-feet.test.ts), `Advisory coverage`

### Fixes applied in commit `8f6fffb`

1. Updated `contentHash` in 4 shard JSONs to match SHA-256 of actual PNG on disk
2. Changed `widthFt: 6.49 → 5.93` in `src/shared/data/set-pieces.json` for shop-table L0 in both `welcome-room` and `welcome-room-v2`

### Verification

- Both failing tests pass locally: `asset-integrity.test.ts` (21/21) and `set-piece-declared-feet.test.ts` (3/3)
- `npm run check:asset-integrity` → 0 blocking findings
- CI run 30771251320 is in progress with **no failures** on all completed checks

## Current state

- Repair commit `8f6fffb` pushed to branch `assets/batch-20260731-211311`
- CI run 30771251320 running on new HEAD — all early checks passing
- Auto-merge arming blocked by network restrictions (external API calls blocked in CI environment)
- CI Recovery automation (`ci-owner-pr-2498` label, `status: dispatched`) will arm auto-merge when CI goes green

## What remains

- Wait for CI run 30771251320 to complete (Unit Tests, Lightweight Checks, Advisory coverage, E2E Visual, Headless Floor 1 Gate still in progress)
- If any check fails unexpectedly, investigate from actual job logs
- CI Recovery automation should arm auto-merge and the Merge Train will handle promotion

## Notes

- No review threads to resolve (0 open threads)
- The `opaqueBounds` in `welcome-room-shop-table-var-0.json` remain technically invalid (y+h > canvasHeight) — this is a data quality issue in the shard but doesn't block merge; it just means the sprite renders at full-canvas scale rather than opaque-bounds scale. A future art regeneration pass would produce valid bounds.
