# Session Handoff: PR 2851 CI Recovery

## Date

2026-08-13

## Persona

Producer coordinating QA/Game repair

## Systems touched

mapgen, quests, ci-policy

## Apples

2🍎 estimated, 2🍎 actual.

## Problem

PR #2851 was blocked by two review threads on the Floor 1 staged-lock placement
fix and an Advisory coverage timeout in
`tests/game/floor1-quest-tour-length.test.ts`.

## What Was Done

- Made lock-aware placement treat pre-seal special-room perimeter gaps as
  blocked, matching the topology after `sealSpecialRooms` turns those gaps into
  walls or locked doors.
- Changed the Floor 1 alignment pass to fail floor construction explicitly when
  no lock-reachable quest item / Slime Rat pair exists, rather than retaining
  `chooseObjectiveTiles`' unvalidated fallback.
- Raised the long Floor 1 quest-tour test timeout for coverage runs; the
  100-seed staged-lock coverage now has headroom under V8 instrumentation.

## Validation

- `bash scripts/agent/preflight.sh`: passed.
- `npm run typecheck`: passed.
- `npx vitest run tests/game/floor1-quest-tour-length.test.ts`: passed.
- Targeted coverage run for the same test file: tests passed in 183.7s; the
  command exits non-zero when run alone because repo-wide coverage thresholds are
  not meaningful for a single-file coverage run.

## Blockers

None known.
