node_modules/
dist/
coverage/
*.local
.DS_Store
.tsbuildinfo
.tsbuildinfo-*
.vitest-cache/
.eslintcache
.cache/
tmp/
artifacts/experiments/

# TypeScript in-place compiled outputs (scripts and source dirs that use tsc without outDir)
scripts/sprites/**/*.js
scripts/sprites/**/*.js.map
scripts/sprites/**/*.d.ts
scripts/sprites/**/*.d.ts.map
src/devtools/*.js
src/devtools/*.js.map
src/devtools/*.d.ts
src/devtools/*.d.ts.map
src/engine/sprites/*.js
src/engine/sprites/*.js.map
src/engine/sprites/*.d.ts
src/engine/sprites/*.d.ts.map

# Sprite-generation pipeline scratch artifacts (raw candidates, processed
# variants, judge scorecards). Approved sprites live under public/assets/
# generated/ and ARE checked in. See .specify/specs/sprite-generation-pipeline.md.
generated/
!public/assets/generated/
!public/assets/generated/**

# The aggregate manifest is a BUILD ARTIFACT composed from the per-asset shards
# under public/assets/generated/entries/ (see tools/vite-plugin-generated-manifest.ts
# and scripts/sprites/build-manifest.ts). It is intentionally NOT committed:
# keeping it out of git is what lets two art check-ins touching different assets
# never conflict on a shared mega-file. This ignore must come AFTER the
# !public/assets/generated/** negation above so it wins.
public/assets/generated/manifest.json

_check-merge.sh


# Local automation report drops
.automation-reports-local/
files/guard-telemetry.jsonl
files/producer-orchestration.jsonl

# Session/dev artifacts (visual-review screenshots + JSON, scratch captures, logs).
# Convention: files/ holds per-session artifacts that are never committed.
files/

# Local environment credentials (populate via scripts/setup-azure-env.ps1)
# Variable names documented in scripts/azure-env.example.
# .env.local is already covered by the *.local rule above; listed explicitly for clarity.
.env.local
.env

# Basic Memory local index/artifacts (see docs/guides/agent-memory.md)
**/.basic-memory/

# Codex repair local runtime artifacts (prompt/context/results)
.github/scripts/codex/runtime/
.env.codex.local

# actionlint binary (downloaded for local use; not committed)
actionlint

# Playwright MCP session artifacts (console logs, traces) - throwaway
.playwright-mcp/

# Stryker mutation testing artifacts (sandbox + HTML/JSON reports)
.stryker-tmp/
reports/
.grade-scratch/
