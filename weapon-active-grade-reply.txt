```json
{
  "criteria": {
    "correctness": 2,
    "scope_discipline": 5,
    "test_coverage": 4,
    "policy_compliance": 5,
    "maintainability": 4
  },
  "verdict": "fail",
  "findings": [
    {
      "severity": "blocker",
      "file": "src/game/playerCarryover.ts",
      "detail": "migrateRetiredArcaneSkillAbilities only migrates Arcane. The other 10 weapon types (e.g. 'sword-strike-base' -> 'sword-strike-active') were also converted to actives and had their IDs renamed, but are omitted from migration. Because they are omitted, existing saves will retain the deprecated zero-value passive, miss the new active, and fail to revoke the passive at level 15. Rename the migration function to cover all weapons, expand the 'replacements' array to include all weapon types, and parameterize the hardcoded 'arcane' skillId in skillAbilityGrantSourceId(skillId, milestoneLevel)."
    }
  ],
  "notes": "The implementation of the new active damage pipeline and the runtime prerequisite checks are very clean. However, the migration logic in playerCarryover.ts only targets Arcane, completely missing the other 10 weapon types modified by this PR. This will permanently break milestone rewards for any in-progress player saves that have already unlocked these skills. This must be addressed before merge."
}
```
