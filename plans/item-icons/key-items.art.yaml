id: item-icons-key-items
title: 'Item Icons — Key Items'
summary: 'Backlog of key-item inventory icons that currently render procedural placeholder PNGs (manifest sourceRun "placeholder"). Each entry needs real authored pixel art generated via the sprite pipeline when provider access is available.'
assets:
  - id: floor-key-silver
    type: item
    label: 'Silver Floor Key'
    brief: 'Silver Floor Key — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. A slightly fancier door opener.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: floor-key-silver

  - id: floor-key-gold
    type: item
    label: 'Gold Floor Key'
    brief: 'Gold Floor Key — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Opens doors and impresses audiences.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: floor-key-gold

  - id: floor-key-void
    type: item
    label: 'Void Floor Key'
    brief: 'Void Floor Key — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. The lock dissolves when this approaches.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: floor-key-void

  - id: directors-pass
    type: item
    label: "Director's Pass"
    brief: "Director's Pass — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. All-access backstage pass. Very exclusive."
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: directors-pass

  - id: map-fragment-a
    type: item
    label: 'Map Fragment A'
    brief: 'Map Fragment A — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Shows part of the next floor.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: map-fragment-a

  - id: map-fragment-b
    type: item
    label: 'Map Fragment B'
    brief: 'Map Fragment B — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Another piece of the puzzle.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: map-fragment-b

  - id: map-fragment-c
    type: item
    label: 'Map Fragment C'
    brief: 'Map Fragment C — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Three fragments, one truth.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: map-fragment-c

  - id: sponsor-contract
    type: item
    label: 'Sponsor Contract'
    brief: 'Sponsor Contract — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Read the fine print. Always.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: sponsor-contract

  - id: audience-token
    type: item
    label: 'Audience Token'
    brief: 'Audience Token — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Grants one audience vote override.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: audience-token

  - id: season-badge
    type: item
    label: 'Season Badge'
    brief: 'Season Badge — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Proof you survived this far.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: season-badge

  - id: boss-trophy
    type: item
    label: 'Boss Trophy'
    brief: 'Boss Trophy — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Taken from something that wanted to keep it.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: boss-trophy

  - id: ancient-cipher
    type: item
    label: 'Ancient Cipher'
    brief: 'Ancient Cipher — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. The Gradient left this behind. Decrypt it.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: ancient-cipher

  - id: green-room-key
    type: item
    label: 'Green Room Key'
    brief: "Green Room Key — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Access to the safe room's VIP area."
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: green-room-key

  - id: producers-note
    type: item
    label: "Producer's Note"
    brief: "Producer's Note — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Hints at what the next floor holds."
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: producers-note

  - id: broadcast-chip
    type: item
    label: 'Broadcast Chip'
    brief: 'Broadcast Chip — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Contains a recording. Of what?'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: broadcast-chip

  - id: elevator-fuse
    type: item
    label: 'Elevator Fuse'
    brief: "Elevator Fuse — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. The elevator won't move without it."
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: elevator-fuse

  - id: vip-lanyard
    type: item
    label: 'VIP Lanyard'
    brief: 'VIP Lanyard — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Looks important. Feels important.'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: vip-lanyard

  - id: classified-dossier
    type: item
    label: 'Classified Dossier'
    brief: 'Classified Dossier — a single centered key item icon with a clear, readable silhouette on a transparent background. Favor a distinct keepsake, key, token, or document silhouette that signals story importance. Eyes only. (Everyone reads it anyway.)'
    placeholderInUse: true
    integration:
      kind: item-catalog
      id: classified-dossier
